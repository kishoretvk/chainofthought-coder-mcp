# Core dependencies for ChainOfThought Coder V2
# Production-ready requirements

# MCP SDK - Required for MCP server functionality
mcp>=1.0.0,<2.0.0

# Database - SQLite3 is part of Python standard library (no version needed)
# sqlite3 is built into Python 3.8+

# JSON processing
json-log-formatter>=0.5.0

# Async support (if needed for older Python versions)
# asyncio is built into Python 3.4+

# Optional: For enhanced performance
# msgpack>=1.0.0  # MessagePack serialization
# zstandard>=0.18.0  # Compression

# Optional: For cloud storage integration
# boto3>=1.26.0  # AWS S3 backups
# google-cloud-storage>=2.0.0  # Google Cloud Storage

# Optional: For metrics and monitoring
# prometheus-client>=0.16.0  # Prometheus metrics
# structlog>=22.0.0  # Structured logging

# Optional: For encryption
# cryptography>=39.0.0  # AES-256 encryption

# Development dependencies (not required for production)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
